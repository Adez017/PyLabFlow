# pytest.ini
[pytest]
pythonpath = src
testpaths = tests
addopts = -ra
markers =
    integration: marks tests that validate cross-module behavior and filesystem/database interactions
filterwarnings =
    ignore::DeprecationWarning:jupyter_client.connect
